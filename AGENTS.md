# YamaLens リポジトリ指示

## 正本となる文書

- `doc/YamaLens_事前決定事項.md` を、プロダクト判断における最優先の決定記録として扱う。
- スコープと受け入れ条件は `doc/登山コンシェルジュアプリ_要求仕様書.md`、アーキテクチャと実装順序は `doc/YamaLens_基本設計書.md`、操作と見た目は `doc/YamaLens_UI設計書.md` に従う。
- UIの作成・変更前に `doc/ui_images/` の画像を補助的な参考資料として確認する。ただし画像は正本や完成見本ではなく、画像内の画面構成、文言、機能、色、部品をそのまま仕様化・模倣しない。画像より上記の正本文書、iOS 26の標準UI、アクセシビリティ、対象画面の目的を優先し、画像に引っ張られずYamaLensとして設計する。
- 文書間で矛盾がある場合は、上記の優先順位で判断して矛盾を報告する。暗黙に別の仕様を作らない。
- プロダクト判断やユーザーから見える動作を変更する場合は、同じ変更内で関連文書も更新する。

## プロダクトの境界

- Xcode 26、Swift、SwiftUIを使用し、iOS 26を搭載したiPhone 15 Pro／15 Pro Max以降のProモデル向けに開発する。
- MVPの初期正式対応は丹沢に限定する。AR現地検証のため、正本に従って山頂・DEM中心の開発用テストパックを丹沢外へ追加できるが、正式対応や施設情報の網羅を意味しない。事前決定事項を変更しない限り、ルート案内、登山地図、GPS活動記録、SNS、アカウント、クラウド同期、AI要約、Apple Watch、Dynamic Islandを追加しない。
- カメラや位置情報を許可しなくても、一覧、検索、山詳細、保存済み情報、メモ、お気に入りを利用できるようにする。
- カメラによる山の表示は、端末内の現在地、真北方位、端末姿勢、画角、山頂データ、任意の地形データから計算した「候補」として扱う。確実な認識結果として表示しない。
- 経路案内はYamaLens内で実装しない。Appleマップを既定とし、利用可能な場合のみGoogle Mapsも選択肢にする。
- よく使う出発駅を1件だけ端末内へ登録し、「現在地」または登録駅を外部地図の出発地にできるようにする。この経路操作だけを理由にYamaLensの位置情報権限を要求しない。
- 山詳細にはWeatherKitの前日の山頂付近日別サマリーを現在・予報と区別して表示する。実測、登山道状態、安全判断として表現しない。

## 安全性、プライバシー、データ保全

- 登山が安全か、実行すべきかを断定しない。事実、不確かさ、情報の鮮度、公式情報へのリンクを示す。
- 天気など変動する情報には、提供元と取得日時または確認日を表示する。未取得・古い情報を明示し、推測値で補完しない。
- カメラ映像、正確な位置履歴、メモ、お気に入り、よく使う出発駅は端末内で扱う。駅の検索語と検索履歴は保存・通常ログ出力しない。カメラ、位置情報、モーションの権限は、該当機能を利用する直前に要求する。
- 秘密情報やAPIキーをアプリへ埋め込まない。MVPへ分析SDKを追加しない。施設情報の自動取得は、正本文書で承認した公式情報源をパック作成工程から低頻度で取得する場合だけ許可し、汎用的・無差別なスクレイピングやアプリからの直接巡回を追加しない。
- オフラインパックを削除してもメモとお気に入りを保持する。手動保存されたパックを自動削除しない。
- 通信はHTTPSとApp Transport Securityの標準設定を使用し、ATS例外や独自の証明書信頼処理を追加しない。外部URLとダウンロードデータは事前決定事項13.9節の上限・形式を検証してから使用する。
- 個人データと診断ログに規定のFile Protectionを適用する。診断ログは明示操作時だけ保存し、通常ログ・テスト・共有へ正確な位置やメモを無断で含めない。

## エンジニアリング要件

- Xcodeプロジェクトの作成、ファイル追加・移動、アーキテクチャ変更では、基本設計書3章と `.agents/skills/develop-yamalens/references/project-structure.md` を適用する。
- Swiftコードの作成・変更・レビューでは、`.agents/skills/develop-yamalens/references/swift-coding-rules.md` を適用する。
- SwiftUI画面、ナビゲーション、操作部品、カメラオーバーレイの作成・変更・レビューでは、UI設計書と `.agents/skills/develop-yamalens/references/liquid-glass-ui-rules.md` を適用する。
- 動作変更、テスト作成・修正、回帰確認、受け入れ確認では、基本設計書8.3節と `.agents/skills/develop-yamalens/references/testing-strategy.md` を適用する。
- 機能単位のレイヤードアーキテクチャを使用し、依存方向を `Features → Domain ← Infrastructure` とする。具象依存は `App` のComposition Rootで組み立てる。
- XcodeプロジェクトはBundle Identifier `com.kiiisy.YamaLens`、Deployment Target iOS 26.0、Swift 6、Automatic Signingで作成する。Apple Developer Teamは共有設定へ固定しない。
- センサー取得とフレームワーク用アダプターを、純粋な候補算出ロジックから分離する。UI、ドメインロジック、永続化、外部データソースを交換・テスト可能に保つ。
- Apple標準フレームワークを優先する。タスクで明示的に必要とされ、トレードオフを記録した場合を除き、本番用の外部依存を追加しない。
- 本番コードで `!`、`try!`、`as!` を使用せず、未取得・不明な数値を `0` で代用しない。
- Swift 6の並行性チェックに従い、UI状態はMainActor、共有可変状態は単一所有者またはActorへ隔離する。安全性検査を無効化して回避しない。
- 読み込み中、空、古い情報、オフライン、権限拒否、低精度、パック破損、更新失敗を明示的な状態として設計する。
- エラー分類、タイムアウト、再試行、クラッシュ復旧は事前決定事項13.6〜13.7節に従う。署名・整合性エラーを自動再試行せず、SwiftDataストアを自動削除・初期化しない。
- 個人データはSwiftData、オフライン構造化データは読み取り専用SQLite、地形はLZFSE圧縮タイル、気象は分離した端末キャッシュへ保存する。パック操作から個人データを変更しない。
- 気象鮮度、注意表示、候補算出には事前決定事項13章の初期値を使用し、一つの設定値型へ集約する。値を変更する場合は正本文書、境界テスト、固定リプレイ、理由を同時に更新する。
- 前日気象の対象日、6時間キャッシュ、欠損処理と、登録駅・外部地図の動作は事前決定事項13.4・13.11節に従う。通知はMVP後とし、明示的なスコープ変更なしにUserNotificationsや通知権限を追加しない。
- すべてのUI変更でDynamic Type、VoiceOver、十分なコントラスト、色だけに依存しない状態表示、屋外でも操作しやすいタップ領域に対応する。
- iOS 26のLiquid Glassを標準デザイン言語とし、SwiftUI標準部品を優先する。ガラス効果はナビゲーションと重要な操作レイヤーへ限定し、情報コンテンツの可読性を優先する。
- 動作変更には、対象を絞ったテストを追加または更新する。候補算出には決定的なユニットテストを用意し、重要なSwiftUI状態にはプレビューまたは状態駆動テストを用意する。
- 自動テストで実ネットワーク、現在時刻、現在地、実センサー、テスト順序を前提にしない。実機・認証情報・現地でしか確認できない項目を自動テスト済みと表現しない。
- 実装中は対象を絞ったテストを実行し、引き渡し前に利用可能なプロジェクト全体のビルドとテストを実行する。Xcode、シミュレーター、実機、認証情報、現地検証の不足により確認できない事項を明記する。

## 作業方法

- 機能実装、アーキテクチャ変更、UI、データモデル、オフライン動作、センサー・カメラ、受け入れ確認には `$develop-yamalens` を使用する。
- ブランチ作成、コミット、Pull Request、マージ、タグ、リリースでは、`.agents/skills/develop-yamalens/references/git-workflow.md` を適用する。
- GitHub Actions、CI、必須Status Check、自動化用Secretでは、`.agents/skills/develop-yamalens/references/ci-strategy.md` を適用する。
- 基本設計書の9章に従い、小さな垂直スライスで実装する。将来機能を仮の本番抽象化として先行実装せず、明示的なスコープ判断の後に追加する。
- 文書化された受け入れ条件に証拠を対応付けずに、MVPや個別の受け入れ条件を完了扱いにしない。
- Swiftの知識を前提にせず、実装結果を平易な日本語で説明する。技術的な選択を求める場合は、推奨案、理由、利点、欠点、ユーザーへの影響を先に示す。
- 引き渡し時は「何ができるようになったか」「なぜこの設計か」「どのように検証したか」「未確認事項は何か」を区別して報告する。

## Git運用

- `main` だけを長期ブランチとするGitHub Flowを使用し、常にビルド・検証可能な状態を保つ。
- 一つの論理的な変更ごとに短命な作業ブランチを作り、Pull RequestをSquash mergeして作業ブランチを削除する。`develop`、`release`、`hotfix`の常設ブランチは作らない。
- `main` へのforce push、履歴の書き換え、未検証変更の直接pushを行わない。不具合の取り消しはrevertで行う。
- ユーザーが明示的に依頼しない限り、ブランチ作成・切替・削除、コミット、push、Pull Request作成、マージ、タグ作成を行わない。
- 仕様変更では正本文書、実装、テストを同じPull Requestに含め、受け入れ条件と検証証拠をPull Requestへ記載する。

## CI運用

- Pull Request、`main` へのpush、手動実行でGitHub Actionsを動かす。古い同一ブランチの実行はキャンセルする。
- 現在はリポジトリ構成、スキル、内部リンク、追跡禁止ファイルを検証する。Xcodeプロジェクト作成と同じ変更で、macOS 26、Xcode 26.6、iOS 26.5シミュレーターによるビルド・テストを追加する。
- Workflowの`GITHUB_TOKEN`権限は`contents: read`を既定とし、必要なJobだけ最小限に追加する。Pull Request由来コードへ書き込み権限やSecretを渡さない。
- 外部Actionは完全なcommit SHAへ固定し、Dependabotで更新する。CI内で未固定のスクリプトやパッケージを無条件にダウンロードして実行しない。
- CIは実機、カメラ、真北方位、モーション、WeatherKit実接続、外部地図、複数山域および丹沢での現地検証の代替にしない。

## コードレビュー規則

- Swiftコーディング規則への違反、特に強制アンラップ、エラーの握りつぶし、未取得値の `0` 置換、単位が不明な生の数値、UI内のI/O、テスト不能なグローバル状態を指摘する。
- 一覧、検索、山詳細、メモ、保存済みオフライン情報の利用に、カメラや位置情報の許可を必須とする実装を指摘する。
- カメラ候補を確定結果として表現する実装、安全性を断定する実装、古いデータを黙って表示する実装、公式情報へのリンク不足、パック操作で個人データを失う実装を指摘する。
- Liquid Glassを情報カードや候補ラベルへ過剰適用する実装、標準SwiftUI部品を不要に再実装する実装、透明度・コントラスト・動きのアクセシビリティ設定で読めなくなる実装を指摘する。
- 決定的なリプレイテストを妨げる形で、センサーやフレームワークのコードが候補順位付けロジックに混在している場合は指摘する。
- 気象や候補算出の閾値が画面・アダプター・計算処理へ直書きされている場合、または初期値の変更に境界テストとリプレイ証拠がない場合は指摘する。
- 前日サマリーを現在予報・実測・登山道状態と混同する実装、欠損を0mmへ置換する実装、経路検索のためだけに位置権限を要求する実装、登録駅や検索語を通常ログへ出す実装を指摘する。
- ATS例外、未検証のURL・ファイルサイズ・offset・SQL、個人ストアの自動初期化、診断ログの無断保存・共有、実装と一致しないPrivacy Manifestを指摘する。
- 動作やスコープを変更したのに、4つの仕様文書が整合していない場合は指摘する。
